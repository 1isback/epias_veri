export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  EXPLORER: '/explorer',
  COMPANY: '/company',
} as const;

export const APP_CONFIG = {
  APP_NAME: 'Energy Intelligence',
  DEFAULT_LOCALE: 'en-US',
  DASHBOARD_REFRESH_INTERVAL: 60000,
} as const;
